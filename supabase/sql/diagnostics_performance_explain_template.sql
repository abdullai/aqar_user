-- =============================================================================
-- قالب EXPLAIN ANALYZE — للصق استعلام PostgREST / SQL الفعلي بعد استخراجه
-- =============================================================================
-- المصادر المحتملة:
--   - Supabase → Logs → Postgres / API
--   - تفعيل log_min_duration_statement مؤقتاً (بحذر) في إعدادات قاعدة البيانات
--   - نسخ الـ URL query من عميل Flutter (إن وُجدت أداة تتبع)
--
-- استبدل الجسم أسفل EXPLAIN بالاستعلام الكامل ثم شغّل مرة واحدة.
-- =============================================================================

-- استبدل السطر التالي بالاستعلام الكامل من السجلات، واحتفظ بـ EXPLAIN أعلاه.
EXPLAIN (ANALYZE, BUFFERS, VERBOSE)
SELECT 1 AS replace_me_with_full_query_from_logs;
