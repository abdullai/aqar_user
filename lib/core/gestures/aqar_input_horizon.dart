import 'package:flutter/material.dart';

/// كان شريطاً مائياً فوق لوحة المفاتيح. حارس الكيبورد يخفيه دائماً.
class AqarInputHorizon extends StatelessWidget {
  const AqarInputHorizon({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
